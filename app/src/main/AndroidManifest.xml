<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kotlinnfcmanager">
        <activity
            android:name=".DashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="Secure Access" />
        
        <activity 
            android:name=".RegisterActivity"
            android:exported="false"
            android:label="Register Card" />

        <activity
            android:name=".VCardActivity"
            android:exported="false"
            android:label="Digital vCard" />

        <activity
            android:name=".EMoneyActivity"
            android:exported="false"
            android:label="E-Money Reader" />

        <activity
            android:name=".EmulationActivity"
            android:exported="false"
            android:label="Identity Emulation" />

        <activity
            android:name=".ClonerActivity"
            android:exported="false"
            android:label="NFC Cloner" />

        <activity
            android:name=".WriterActivity"
            android:exported="false"
            android:label="Tag Writer" />

        <service
            android:name=".NfcHceService"
            android:exported="true"
            android:label="@string/hce_service_desc"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/apduservice" />
        </service>
    </application>

</manifest>